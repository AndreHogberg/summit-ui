@namespace ArkUI.Components.Select
@typeparam TValue where TValue : notnull

<span data-ark-select-value="" data-placeholder="@(IsPlaceholder ? "" : null)">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else
    {
        @DisplayText
    }
</span>
