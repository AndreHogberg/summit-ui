@namespace ArkUI.Components.Tabs
@inherits Microsoft.AspNetCore.Components.ComponentBase
@using Microsoft.AspNetCore.Components.Rendering

@{
    BuildElement(__builder);
}

@code {
    private void BuildElement(RenderTreeBuilder builder)
    {
        builder.OpenElement(0, As);
        builder.AddAttribute(1, "role", "tablist");
        builder.AddAttribute(2, "aria-orientation", Context.Orientation.ToString().ToLowerInvariant());
        builder.AddAttribute(3, "data-orientation", Context.Orientation.ToString().ToLowerInvariant());
        builder.AddAttribute(4, "data-ark-tabs-list", true);
        builder.AddMultipleAttributes(5, AdditionalAttributes);
        builder.AddElementReferenceCapture(6, elementRef => _elementRef = elementRef);
        builder.AddContent(7, ChildContent);
        builder.CloseElement();
    }
}
